export const metadata = {
  title: "Contact Us - Send a message | TRI-J SECURITY AGENCY",
};

import ContactClient from "../components/contactclient";

export default function Contact() {
  return <ContactClient />;
}
